#!/bin/sh

getvol() {
	echo "1"
}

setvol() {
	echo "2"
}

toggle() {
	wpctl set-mute "@DEFAULT_AUDIO_SINK@" toggle || {
		echo "Failed with wpctl, retrying with pactl."
		pactl set-sink-mute "@DEFAULT_SINK@" toggle
	}
}

case $1 in
	get) getvol ;;
	set) setvol $@ ;;
	toggle) toggle ;;
esac
