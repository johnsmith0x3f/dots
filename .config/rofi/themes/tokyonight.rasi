// Rofi v1.7.5

configuration {
	modes:                 [ "drun" , "run" ];
	font:                  "FiraMono Nerd Font Mono 18";
	display-drun:          "󱓞 App";
	display-run:           "󰞷 Run";
	drun-display-format:   "{name}";
	show-icons:            true;
}

* {
	background:            #1F2335;
	background-alt:        #24283B;
	foreground:            #C0CAF5;
	selected:              #3B4261;
	active:                #3D59A1;
	urgent:                #FF595E;

	blue:                  #7AA2F7;
	green:                 #C3E88D;
}

window {
	height:                600px;
	background-color:      @background;
}

mainbox {
	background-color:      transparent;
	children:              [ "mode-switcher", "inputbar", "listbox" ];
}
mode-switcher, inputbar, listbox {
	padding:               24px;
	background-color:      transparent;
}

mode-switcher {
	spacing:               24px;
} // children: [ "button", ..., "button" ];

button {
	padding:               12px;
	background-color:      transparent;
	text-color:            @foreground;
	cursor:                pointer;
}
button.selected {
	border-radius:         12px;
	background-color:      @green;
	text-color:            @background;
}

inputbar {
	border-radius: 			   12px;
	text-color:            @foreground;
	spacing:               12px;
	children:              [ "prompt", "entry" ];
}

prompt {
}

entry {
	background-color:      inherit;
	text-color:            inherit;
	cursor:                text;
	placeholder:           "Search";
}

listbox {
	children:              [ "listview" ];
}

listview {
	background-color:      transparent;
	text-color:            @foreground;
	spacing:               12px;
} // children: [ "element", ..., "element" ];

element {
	spacing:               12px;
	padding:               12px;
	border-radius:         12px;
	background-color:      transparent;
	text-color:            @foreground;
	cursor:                pointer;
}
element.selected {
	background-color:      @selected;
	text-color:            @foreground;
} // children: [ "element-icon", "element-text" ];

element-icon {
	background-color:      transparent;
	size:                  32px;
}
element-text {
	background-color:      transparent;
	text-color:            inherit;
	vertical-align:        0.5;
}
